type Wallet @entity(immutable: false) {
  id: ID! # vault_owner
  vault: Bytes! # address
  owner: Bytes! # address
  deposited: BigInt!
  withdrawn: BigInt!
}

type Variable @entity(immutable: false) {
  id: ID!
  value: BigInt!
}
